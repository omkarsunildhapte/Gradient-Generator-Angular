import { DegreeDirective } from './degree.directive';

describe('DegreeDirective', () => {
  it('should create an instance', () => {
    const directive = new DegreeDirective();
    expect(directive).toBeTruthy();
  });
});
